import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  wrapper: {
    width: '100%',
    marginVertical: 10,
  },
  input: {
    backgroundColor: '#dcdde1',
    width: '100%',
    height: 50,
    padding: 10,
    borderRadius: 10,
    color: '#000',
    fontSize: 18
  },
  label: {
    fontSize: 20,
    color: '#000',
    marginBottom: 7,
  }
})