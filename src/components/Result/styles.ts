import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  wrapper: {
    width: '100%',
  },
  resultArea: {
    width: '100%',
    marginVertical: 10,
  },
  labelText: {
    fontSize: 17,
    color: '#000'
  },
  productText: {
    fontSize: 25,
    color: '#ff0000'
  },
})